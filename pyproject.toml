[project]
name = "idm-engine"
version = "0.1.0"
description = "Moteur de téléchargement HTTP fiable et reprenable"
requires-python = ">=3.11"

[project.scripts]
idm = "idm.cli:main"

[tool.unittest]
start-directory = "tests"

